import { DescShortnerPipe } from './desc-shortner.pipe';

describe('DescShortnerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescShortnerPipe();
    expect(pipe).toBeTruthy();
  });
});
